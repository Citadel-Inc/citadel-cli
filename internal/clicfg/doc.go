// Package clicfg loads, persists, and resolves the citadel-cli runtime
// configuration (server URL, OAuth tokens, profile UUID) at
// $XDG_CONFIG_HOME/citadel/config.toml or ~/.config/citadel/config.toml.
package clicfg
