# Optional stricter pass than `go vet` (CI). Run `make golangci`.
version: "2"
run:
  timeout: 5m
linters:
  default: standard
  enable:
    - ineffassign
    - misspell
    - unconvert
  settings:
    staticcheck:
      checks: ["all"]
    misspell:
      locale: US
